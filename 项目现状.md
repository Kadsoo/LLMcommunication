# 项目现状

- 课程：大模型智能通信（大三上）
- 本地目录：`学校/大三上学期/大模型智能通信/`，现有 `steps.md` + `Latent Collaboration in Multi-Agent Systems.pdf`
- 远端仓库：https://github.com/Kadsoo/LLMcommunication ，当前为空仓库，未初始化

## 任务来源

`steps.md` 共 10 步：1选任务模型 → 2单机TextMAS → 3通信接口 → 4跨机通信 → 5监控日志 → 6 KV传输 → 7 Token/KV对比 → 8可视化Demo → 9接实验室系统 → 10优化。

参考论文为 LatentMAS（纯隐空间多智能体协作），主要指导 Step6 / Step10。

## 已确认决策（2026-09-04）

- 模型：本地 Qwen/Qwen3-0.6B 调全流程，服务器 Qwen3-4B 出终测（同系切换只改 model_id）
- 机器：现阶段只有本机，后续用两台服务器做真跨机
- 范围：做到 Step8 Web Demo，Step9 只预留接口，Step10 可选
- 策略：单机双进程模拟跨机，调通后再搬到服务器

## 本机实测

- GPU：RTX 4060 Laptop 8GB（当前可用约 6.4GB，系统占用约 1.7GB）
- 内存：32GB，C盘剩余约 230GB
- Python 3.13.5，`transformers 5.16.1` 已装，`torch 2.13.0+cpu`（不支持 CUDA）
- 结论：0.6B（约0.6B参数，CPU可跑通，`1+1=`正常生成）本机可跑通逻辑，4B 等上服务器再测
- 模型预期分工（2026-09-04 确认）：0.6B 只做流程正确性验证（管线/接口/KV等价），GSM8K 攻坚数据以服务器 4B 为准
