| 步骤                | 要做什么                                             | 主要产出              | 是否核心 |
| ----------------- | ------------------------------------------------ | ----------------- | ---- |
| 1. 选任务与模型         | 选一个简单任务，建议先用数学题；两边用同一个小模型                        | 可稳定运行的小模型 + 测试题集  | 必须   |
| 2. 跑通 TextMAS     | 先在单机上让两个 Agent 用文本讨论，例如 A 解答 → B 批评 → A 修正       | 基础多智能体协作流程        | 必须   |
| 3. 建通信接口          | 把 Agent 和通信解耦，统一 `send/recv` 接口，支持 TOKEN / KV 类型 | Transport Adapter | 必须   |
| 4. 两台 PC 跨机通信     | PC A 和 PC B 各跑 Agent，通过 TCP/gRPC 等先传文本           | 真正的跨机 Agent 对话    | 必须   |
| 5. 加监控与日志         | 记录推理时间、传输时间、payload 大小、token 数、带宽等               | Telemetry / 实验数据  | 必须   |
| 6. 实现 KV Cache 传输 | 捕获 `past_key_values`，序列化后从 A 传给 B，并恢复继续推理        | KV-MAS            | 核心技术 |
| 7. 对比 Token / KV    | 分别跑 Token 和 KV 模式，对比成功率、时延、传输量、Prefill 时间        | 实验结果表             | 必须   |
| 8. 做可视化系统         | 展示 Agent 对话、当前通信模式、Token/KV 大小、时延、成功/失败          | 最终 Web Demo       | 最终交付 |
| 9. 接实验室通信系统     | 把原来的 TCP/gRPC Transport 替换成实验室提供的通信接口            | 完整软硬件系统           | 后期   |
| 10. 进一步优化         | 做 Delta KV、自动 Token/KV 切换，甚至 LatentMAS           | 可发论文的扩展           | 可选   |
