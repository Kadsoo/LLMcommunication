# 实施计划（目标：Step8 Demo）

## P0 初始化
- 初始化空仓库目录结构：`agents/ transport/ telemetry/ eval/ demo/`
- 本机验证 Qwen/Qwen3-0.6B 可加载，GSM8K 抽 5 题冒烟；服务器阶段切 4B 重测
- 验证标准：单模型可问答

## P1 Step1-2 单机 TextMAS
- 同一 0.6B 模型分饰 A（解答）→ B（批评）→ A（修正），服务器阶段换 4B
- 产出：`run_textmas.py` + 基线成功率

## P2 Step3 通信接口
- 抽象 `Transport.send/recv`，支持 `TOKEN / KV` 类型切换
- 产出：Agent 不直调 socket，只调接口

## P3 Step4 跨机模拟
- 本机双进程 + TCP 传文本，JSON 消息；后续改 IP 即切双服务器
- 验证：两端日志能对上同一轮对话

## P4 Step5 监控日志
- 记录推理耗时、传输耗时、payload 大小、token 数、KV shape
- 存 JSONL，供对比用

## P5 Step6 KV 传输（最难）
- 传 `past_key_values`，B 端恢复后继续生成，需同模型同版本
- 先做短 prompt + 短生成验证等价性，再放开长度
- 风险：KV 体积大、序列化慢、显存翻倍，必要时单模型复用 + 减层/减长

## P6 Step7 对比
- 同题分别跑 TOKEN / KV，对比成功率、时延、传输量、Prefill
- 产出：实验结果表

## P7 Step8 可视化
- Gradio 单页：对话流 + 模式切换 + Token/KV 大小 + 时延 + 成功失败
- Step9 预留 `LabTransport` 空类，不联调
